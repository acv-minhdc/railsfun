<table>
  <% @products.each do |product| %>
    <tr>
      <td><%= product.title %></td>
      <td><%= product.price %></td>
      <td><%= product.category.try(:name) %></td>
    <tr>
  <% end %>
</table>
